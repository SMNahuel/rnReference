import { Buttons } from '@components';

import type { ButtonProps } from '.';

const SigninGoogle = (props: ButtonProps): JSX.Element => (
  <Buttons.Simple {...props} />
);

export default SigninGoogle;
