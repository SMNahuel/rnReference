import { StyleSheet } from 'react-native';

const btnStyles = StyleSheet.create({
  container: {
    flex: 1,
    borderWidth: 1,
    borderRadius: 10,
    borderColor: '#000',
  },
});

export default btnStyles;
